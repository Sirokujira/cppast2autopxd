[build-system]
requires = ["setuptools>=61", "wheel", "Cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "minizpkg"
version = "0.0.1"
description = "Demo: real miniz C library -> cppast_autopxd .pxd -> Cython -> Python package"
requires-python = ">=3.9"
