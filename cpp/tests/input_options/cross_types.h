// NOT generated: stands in for pcl/types.h — an alias-only sibling header
// whose own generation is out of scope (template metaprogramming), so the
// alias must be substituted away with --typemap where it is USED.
#pragma once
#include <cstdint>
namespace xh {
using myindex_t = std::uint32_t;
}
