# Emitter-mode config for tests/input_options/emit_modes.h.
#
# extern_from is the single-valued config key: the pxd is generated from a
# self-contained mirror header but must declare the REAL include path the
# C++ compiler will use. A second extern_from line here is a located error,
# not a silent last-one-wins.
extern_from = demo/store.hpp
